# Tài liệu dự án

- `adr/`: quyết định kiến trúc ngắn gọn.
- `source/`: snapshot nguồn và provenance để audit refactor.

Tài liệu triển khai theo công cụ nằm trong `adapters/`; tài liệu curriculum nằm trong `subjects/`.
